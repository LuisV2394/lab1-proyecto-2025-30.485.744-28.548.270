tags:
  - Orders
summary: Create a new order
description: Creates a new order record in the system.
operationId: createOrder
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: body
    name: body
    required: true
    schema:
      type: object
      properties:
        episode_id:
          type: integer
        professional_id:
          type: integer
          nullable: true
        requires_authorization:
          type: boolean
          example: false
        type:
          type: string
          enum: ["LABORATORY","IMAGING","PROCEDURE","MEDICATION","OTHER"]
          example: "LABORATORY"
        details:
          type: array
          items:
            type: object
            properties:
              code:
                type: string
              description:
                type: string
              instructions:
                type: string
          example:
            - code: "LAB001"
              description: "Blood Test"
              instructions: "Fasting required"
        priority:
          type: string
          enum: ["normal", "urgent"]
          example: "normal"
      required:
        - episode_id
        - type
        - details
responses:
  201:
    description: Order created successfully
    schema:
      type: object
      properties:
        message:
          type: string
          example: "Order created successfully"
        order_id:
          type: integer
  400:
    description: Invalid input
  500:
    description: Server error