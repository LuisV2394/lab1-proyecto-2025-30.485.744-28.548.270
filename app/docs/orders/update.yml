tags:
  - Orders
summary: Update order status
description: Updates the status of an existing order.
operationId: updateOrderStatus
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: path
    name: order_id
    required: true
    type: integer
  - in: body
    name: body
    required: true
    schema:
      type: object
      properties:
        status:
          type: string
          enum: ["issued", "authorized", "in_progress", "completed", "canceled"]
          example: "completed"
responses:
  200:
    description: Status updated successfully
    schema:
      type: object
      properties:
        message:
          type: string
          example: "Order status updated successfully"
        status:
          type: string
          example: "completed"
  400:
    description: Invalid status or canceled order
  404:
    description: Order not found