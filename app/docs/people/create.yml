tags:
  - People
summary: Create a new person
description: Creates a new person record in the system.
operationId: createPerson
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: body
    name: body
    required: true
    schema:
      type: object
      required:
        - document_number
        - first_name
        - last_name
      properties:
        document_number:
          type: string
          example: "V12345678"
        first_name:
          type: string
          example: "Luis"
        last_name:
          type: string
          example: "Daza"
        gender:
          type: string
          enum: ["M", "F", "OTHER"]
          example: "M"
        birth_date:
          type: string
          format: date
          example: "1990-05-20"
        email:
          type: string
          format: email
          example: "luis.daza@example.com"
        phone:
          type: string
          example: "+584123456789"
        address:
          type: string
          example: "Calle Falsa 123, Caracas, Venezuela"
        emergency_contact:
          type: string
          example: "+584123987654"
        active:
          type: boolean
          example: true

responses:
  201:
    description: Person created successfully
    schema:
      type: object
      properties:
        id:
          type: integer
          example: 1
        document_number:
          type: string
          example: "V12345678"
        first_name:
          type: string
          example: "Luis"
        last_name:
          type: string
          example: "Daza"
        gender:
          type: string
          example: "M"
        birth_date:
          type: string
          example: "1990-05-20"
        email:
          type: string
          example: "luis.daza@example.com"
        phone:
          type: string
          example: "+584123456789"
        address:
          type: string
          example: "Calle Falsa 123, Caracas, Venezuela"
        emergency_contact:
          type: string
          example: "+584123987654"
        active:
          type: boolean
          example: true

  400:
    description: Validation error (missing required fields or invalid format)
    schema:
      type: object
      properties:
        error:
          type: string
          example: "Document number already in use or invalid email format"

  500:
    description: Database error or server error
    schema:
      type: object
      properties:
        error:
          type: string
          example: "Database integrity error"