# agenda/delete.yml
tags:
  - Agenda
summary: Eliminar un bloque de agenda
description: Elimina un bloque específico del sistema.
parameters:
  - in: path
    name: block_id
    required: true
    type: integer
    description: ID del bloque a eliminar
responses:
  200:
    description: Bloque eliminado exitosamente
    schema:
      type: object
      properties:
        message:
          type: string
          example: "Bloque eliminado"
  404:
    description: Bloque no encontrado
  500:
    description: Error interno del servidor